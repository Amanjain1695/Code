def call() {
    image: salesforce/salesforcedx
    pipelines:
        script:
        stage('Test') {
             // -------------------------------------------------------------------------
    // Inside Test
    // -------------------------------------------------------------------------
    
               rc = command "$sfdx version"
        }
}
